---
globs: ["**/*.tsx", "**/*.jsx", "**/components/**/*.ts", "**/components/**/*.js", "**/app/**/*.tsx", "**/pages/**/*.tsx"]
alwaysApply: false
---
# React Patterns

React component architecture patterns: container/presentational split, state location, RSC boundary, Suspense, forms, data fetching, lists, composition, compound components, portals. Extends common-coding-style.md. See skill: `react-patterns` for the full guidance.
