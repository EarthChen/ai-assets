---
paths:
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/components/**/*.ts"
  - "**/app/**/*.ts"
  - "**/pages/**/*.ts"
---
# React Security

React security: XSS via dangerouslySetInnerHTML, unsafe URL schemes, target=_blank rel, Server Action input validation, secret exposure via env vars, CSP, prototype pollution. Extends common-security.md. See skill: `react-security` for the full guidance.
