{
  "$schema": "./third-party.schema.json",
  "description": "Third-party plugins to install via each platform's native plugin system",
  "plugins": [
    {
      "name": "mattpocock-skills",
      "description": "Engineering skills by Matt Pocock - vendored as git submodule at vendor/mattpocock-skills, symlinked into skills/",
      "platforms": {
        "all": {
          "note": "Managed as git submodule, no separate install needed. Run: git submodule update --init"
        }
      }
    },
    {
      "name": "understand-anything",
      "description": "Codebase knowledge graph and analysis",
      "platforms": {
        "claude": {
          "install_cmd": "Managed via ~/.agents/skills/ symlinks to ~/.understand-anything/repo/"
        },
        "codex": {
          "install_cmd": "Managed via ~/.agents/skills/ symlinks to ~/.understand-anything/repo/"
        }
      }
    }
  ]
}
